---
kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: riot
  namespace: matrixorg
spec:
  entryPoints: 
    - web
  routes:
  - match: Host(`riot.frikanalen.no`) 
    kind: Rule
    services:
    - kind: Service
      name: riot
      scheme: http
      port: 8765
